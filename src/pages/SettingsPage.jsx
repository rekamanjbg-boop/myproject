import { PlaceholderPage } from '../components/layout/PlaceholderPage.jsx';

export function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Production configuration"
      title="Settings"
      description="Settings route placeholder. Venue, network, display, and operator preferences will be added in a later phase."
    />
  );
}
